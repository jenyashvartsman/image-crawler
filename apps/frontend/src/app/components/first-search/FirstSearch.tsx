import { Box, Typography } from '@mui/material';

const FirstSearch = () => {
  return (
    <Box display="flex" justifyContent="center">
      <Typography variant="h5">
        Search images thru different search engines
      </Typography>
    </Box>
  );
};

export default FirstSearch;
