// eslint-disable-next-line @typescript-eslint/no-unused-vars
import ImageCard from '../image-card/ImageCard';
import styles from './image-grid.module.scss';

const ImageGrid = () => {
  return (
    <div>
      Image grid <ImageCard />
    </div>
  );
};

export default ImageGrid;
